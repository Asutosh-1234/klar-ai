services:
  db:
    image: pgvector/pgvector:pg16  # replaces postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: klar
    ports:
      - "5432:5432"